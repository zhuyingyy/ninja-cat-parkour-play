# 地面与河水切图

本次直接在 Figma 中使用原 PNG 图像填充裁切、拼接并导出，没有重绘美术。

- `ground-loop.png`：1560 × 466；游戏坐标 y=1094；木桥、石岸、河水同速循环。保留原地面上部 160 px，向下延伸原河水纹理，补足固定剪影后方的水面。左右两屏镜像衔接。
- `footer-static.png`：780 × 244；游戏坐标 y=1400；从原背景按深色轮廓切出，透明部分透出移动水面，深色区域固定。它单独叠在地面画布上，不跟随滚动或受伤震动。
- JUMP 按钮、距离文字继续使用独立 UI，位置不变。

Figma：ground-loop 节点 357:859；footer-static 节点 379:857。两者为单张 PNG 填充，1× PNG 导出。裁切来源保存在隐藏归档区，日常替换只需改同名 PNG。

验证包含：循环跨界时画布无透明空隙、暂停冻结、河水随位移变化、两帧截图的深色区域逐像素相同、按钮内部像素及位置不变、390 × 822 等比例显示和离线 HTML 素材加载。
